#pragma once


namespace AcroEngine
{
	/////////////////////////////////////////////////////////////////////////////
	// @ 프로퍼티.
	/////////////////////////////////////////////////////////////////////////////
	template<typename T> class TProperty
	{
	private:
		T* Value;

	public:
		TProperty(const T& Value)
		{
		}

		TProperty(const T& Value)
		{
		}

		virtual ~TProperty()
		{
		}
	};
}